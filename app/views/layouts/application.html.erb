<!DOCTYPE html>
<html>
  <head>
    <title>BookLoop</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% unless devise_controller? || (controller_name == 'pages' && action_name == 'splash') %>
      <%= render "shared/navbar" %>
    <% end %>


    <% if flash[:notice].present? %>
      <div style="background-color: #fbeaea; color: #862221; padding: 15px 20px; margin: 20px auto; max-width: 600px; border-radius: 8px; font-family: 'Helvetica Neue', sans-serif; text-align: center; box-shadow: 0 2px 6px rgba(0,0,0,0.05);">
        <%= flash[:notice] %>
      </div>
    <% elsif flash[:alert].present? %>
      <div style="background-color: #ffe6e6; color: red; padding: 15px 20px; margin: 20px auto; max-width: 600px; border-radius: 8px; font-family: 'Helvetica Neue', sans-serif; text-align: center; box-shadow: 0 2px 6px rgba(0,0,0,0.05);">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <% if flash[:notice] %>
      <div class="flash notice"><%= flash[:notice] %></div>
    <% end %>
    <% if flash[:alert] %>
      <div class="flash alert"><%= flash[:alert] %></div>
    <% end %>


    <%= yield %>

    <%= javascript_include_tag "controllers", type: "module" %>
  </body>
</html>
