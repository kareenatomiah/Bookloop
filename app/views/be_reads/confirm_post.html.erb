<h2 style="
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 1.5rem auto 0.75rem auto;
  padding: 4px 12px;
  font-size: 1.3rem;
  font-weight: 500;
  color: #7B1E3A;
  font-family: 'Merriweather', serif;
  background-color: #f7e9e9;
  border-radius: 6px;
  box-shadow: 0 2px 4px rgba(123, 30, 58, 0.1);
  width: fit-content;
">
  <i class="fa-solid fa-paper-plane" style="
    color: #7B1E3A;
    font-size: 1rem;
    margin-right: 6px;
    background-color: #fff0f2;
    padding: 3px 5px;
    border-radius: 4px;
  "></i>
  Post your BeRead to the feed?
</h2>


<div class="card" style="max-width: 400px; margin: 0 auto 30px; border-radius: 12px; border: 2px solid #862221; box-shadow: 0 4px 8px rgba(134, 34, 33, 0.2);">
  <div class="card-body text-center">
    <p class="mb-3" style="font-weight: 600; color:#862221;">
      <strong>Caption:</strong> <%= @be_read.caption.presence || "No caption" %>
    </p>

    <% if @be_read.photo.attached? %>
      <%= image_tag @be_read.photo.variant(resize_to_limit: [400, 400]),
                    alt: "BeRead photo",
                    style: "max-width: 100%; border-radius: 12px; border: 1px solid #862221; box-shadow: 0 2px 6px rgba(0,0,0,0.1);" %>
    <% else %>
      <p>No photo attached.</p>
    <% end %>
  </div>
</div>

<div style="text-align: center;">
  <%= form_with url: post_to_feed_be_read_path(@be_read), method: :post, local: true do %>
    <%= submit_tag "Yes, post it!", class: "btn btn-primary", style: "background: #862221; border-color: #862221; padding: 12px 28px; font-size: 16px; border-radius: 8px;" %>
    <%= link_to "No, thanks", user_path(current_user), class: "btn btn-secondary", style: "margin-left: 15px; padding: 12px 28px; font-size: 16px; border-radius: 8px;" %>
  <% end %>
</div>
